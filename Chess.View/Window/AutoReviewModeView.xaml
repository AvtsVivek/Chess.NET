<UserControl x:Class="Chess.View.Window.AutoReviewModeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chess.View.Window"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800"
             
             Loaded="UserControl_Loaded" Unloaded="UserControl_Unloaded"
             >
    <StackPanel Orientation="Horizontal" >
        <Button x:Name="DecrementHighButton" Padding="0" Margin="0 0 5 0" Command="{Binding DecrementHighCommand}" >
            <Label Padding="0" FontSize="16" FontWeight="DemiBold" Margin="10 -2 10 0" Content="{Binding DecrementHighValue}"></Label>
        </Button>
        
        <Button x:Name="DecrementLowButton" Padding="0" Margin="5 0 0 0" Command="{Binding DecrementLowCommand}" >
            <Label Padding="0" FontSize="16" FontWeight="DemiBold" Margin="5 -2 5 0 " Content="{Binding DecrementLowValue}"></Label>
        </Button>

        <TextBlock x:Name="AutoTimeIntervalTextBlock" Padding="0" 
                   FontWeight="DemiBold" FontSize="16" Margin="10 3 10 0" Text="{Binding AutoReviewTimeIntervalStringValue}"></TextBlock>

        <Button x:Name="IncrementLowButton" Padding="0" Margin="0 0 5 0" Command="{Binding IncrementLowCommand}" >
            <Label Padding="0" FontSize="16" FontWeight="DemiBold" Margin="5 -2 5 0 " Content="{Binding IncrementLowValue}"></Label>
        </Button>

        <Button x:Name="IncremenHighButton" Padding="0" Margin="5 0 0 0" Command="{Binding IncrementHighCommand}">
            <Label Padding="0" FontSize="16" FontWeight="DemiBold" Margin="5 -2 5 0 " Content="{Binding IncrementHighValue}"></Label>
        </Button>
    </StackPanel>
</UserControl>
