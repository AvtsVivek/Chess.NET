<mah:MetroWindow x:Class="Chess.View.Window.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:conv="clr-namespace:Chess.View.Converter"
        xmlns:icon="clr-namespace:Chess.View.Icon"
        xmlns:localView="clr-namespace:Chess.View.Window"
        xmlns:statusAndMode="clr-namespace:Chess.View.StatusAndMode"
        xmlns:select="clr-namespace:Chess.View.Selector"
        xmlns:game="clr-namespace:Chess.ViewModel.Game;assembly=Chess.ViewModel"
        xmlns:statusAndModeVm="clr-namespace:Chess.ViewModel.StatusAndMode;assembly=Chess.ViewModel"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:gameModel="clr-namespace:Chess.Model.Game;assembly=Chess.Model"
        d:DataContext="{d:DesignInstance Type=game:ChessGameVM}"
        mc:Ignorable="d"
        ResizeMode="CanResizeWithGrip"
        ShowIconOnTitleBar="True"
        WindowStartupLocation="CenterScreen"
        Background="DarkGray" Icon="/Icon/appIcon2.ico"
        Title="Chess.NET" Height="700" Width="700">

    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="AppThemeMenuItemStyle" TargetType="{x:Type MenuItem}">
                <Setter Property="Height" Value="20" />
            </Style>
            <conv:LabelCounterInvertTransformMultiConverter x:Key="LabelCounterInvertTransformMultiConverter"/>
            <conv:PieceCounterInvertTransformConverter x:Key="PieceCounterInvertTransformConverter"/>
            <conv:BoardInvertTransformConverter x:Key="BoardInvertTransformConverter"/>
            <conv:AppModeConverter x:Key="AppModeConverter"/>
            <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <Storyboard x:Key="fadeWhite">
                <ColorAnimation
                    Storyboard.TargetProperty="BorderBrush.(SolidColorBrush.Color)"
                    Duration="{StaticResource duration}" To="White" />
            </Storyboard>
            <Storyboard x:Key="fadeBlack">
                <ColorAnimation
                    Storyboard.TargetProperty="BorderBrush.(SolidColorBrush.Color)"
                    Duration="{StaticResource duration}" To="Black" />
            </Storyboard>
            <SolidColorBrush x:Key="LabelBackgroundBrush" Color="White"/>
            <Style x:Key="CustomRadioButtonStyle" TargetType="RadioButton">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                <Grid Width="16" Height="16">
                                    <!-- Outer circle -->
                                    <Ellipse
                                        Width="16" Height="16"
                                        Stroke="Gray" StrokeThickness="2"
                                        Fill="White" />
                                    <!-- Inner circle, visible only when checked -->
                                    <Ellipse
                                        x:Name="InnerCircle"
                                        Width="8" Height="8"
                                        Fill="DarkBlue"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Visibility="Collapsed" />
                                </Grid>
                                <ContentPresenter Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentStringFormat="{TemplateBinding ContentStringFormat}"
                                                  Margin="0,5,0,0"
                                                  HorizontalAlignment="Center"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="InnerCircle" Property="Visibility" Value="Visible"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Ctrl" Command="{Binding NewCommand}" />
        <KeyBinding Key="Z" Modifiers="Ctrl" Command="{Binding UndoCommand}" />
        <KeyBinding Key="Y" Modifiers="Ctrl" Command="{Binding RedoCommand}" />
    </Window.InputBindings>

    <mah:MetroWindow.IconTemplate>
        <DataTemplate>
            <iconPacks:PackIconGameIcons Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" 
                Margin="4" Foreground="{DynamicResource MahApps.Brushes.IdealForeground}" Kind="ChessQueen" />
        </DataTemplate>
    </mah:MetroWindow.IconTemplate>

    <mah:MetroWindow.TitleTemplate>
        <DataTemplate>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <TextBlock Margin="0 -1 8 0"
                           VerticalAlignment="Center"
                           FontFamily="{DynamicResource MahApps.Fonts.Family.Window.Title}"
                           FontSize="{DynamicResource MahApps.Font.Size.Window.Title}"
                           Text="{TemplateBinding Content}"
                           TextTrimming="CharacterEllipsis" />

            </StackPanel>
        </DataTemplate>
    </mah:MetroWindow.TitleTemplate>

    <mah:MetroWindow.LeftWindowCommands>
        <mah:WindowCommands>
            <Button Click="LaunchGitHubSite" ToolTip="Open up the GitHub site">
                <iconPacks:PackIconModern Width="22"
                                  Height="22"
                                  Kind="SocialGithubOctocat" />
            </Button>
        </mah:WindowCommands>
    </mah:MetroWindow.LeftWindowCommands>

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <Button Click="DeployCupCakes" Content="Deploy CupCakes">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <iconPacks:PackIconModern Width="22"
                                        Height="22"
                                        VerticalAlignment="Center"
                                        Kind="FoodCupcake" />
                            <TextBlock Margin="4 0 0 0"
                         VerticalAlignment="Center"
                         Text="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <DockPanel>
        <DockPanel.Resources>
            <conv:StatusConverter x:Key="statusConverter" />
        </DockPanel.Resources>

        <Grid DockPanel.Dock="Top" Background="White">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="50" />
            </Grid.RowDefinitions>

            <Menu>
                <Menu.ItemsPanel>
                    <ItemsPanelTemplate>
                        <DockPanel HorizontalAlignment="Stretch"/>
                    </ItemsPanelTemplate>
                </Menu.ItemsPanel>
                <MenuItem Header="Game Menu" Margin="10" HorizontalAlignment="Left">
                    <MenuItem Header="New" Command="{Binding NewCommand}" InputGestureText="Ctrl+N" 
                          Style="{StaticResource AppThemeMenuItemStyle}">
                        <MenuItem.Icon>
                            <icon:NewIcon />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Undo" Command="{Binding UndoCommand}" InputGestureText="Ctrl+Z"
                          Style="{StaticResource AppThemeMenuItemStyle}">
                        <MenuItem.Icon>
                            <icon:UndoIcon />
                        </MenuItem.Icon>
                    </MenuItem>
                    <MenuItem Header="Redo" Command="{Binding RedoCommand}" InputGestureText="Ctrl+Y"
                          Style="{StaticResource AppThemeMenuItemStyle}">
                        <MenuItem.Icon>
                            <icon:RedoIcon />
                        </MenuItem.Icon>
                    </MenuItem>
                    <Separator />
                    <MenuItem Header="Exit" Click="ExitClick" 
                          Style="{StaticResource AppThemeMenuItemStyle}">
                        <MenuItem.Icon>
                            <icon:ExitIcon />
                        </MenuItem.Icon>
                    </MenuItem>
                </MenuItem>
            </Menu>

            <statusAndMode:MessageView Grid.Column="1" Margin="5" 
                                   DataContext="{Binding HeaderNotificationMessage}"
                                   HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

            <Button Grid.Column="2" Margin="0 0 5 0"  VerticalAlignment="Center" Command="{Binding BoardInversionToggleCommand}" ToolTip="Invert the board">
                Invert Board
            </Button>


            <Menu Grid.Column="3" >
                <MenuItem HorizontalAlignment="Right">
                    <MenuItem.Header>
                        <StackPanel Orientation="Horizontal">
                            <ContentControl Margin="5" Content="{Binding ModeAndPlayerStatusDisplayVM}" />
                        </StackPanel>
                    </MenuItem.Header>
                </MenuItem>
            </Menu>
        </Grid>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="ChessGameColumn" Width="*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition x:Name="ConsoleColumn" Width="*" />
            </Grid.ColumnDefinitions>

            <Viewbox Margin="10" x:Name="MainBoardViewBox" Focusable="True" >
                <Canvas Width="{x:Static game:BoardConstants.FullCanvasLength}" Name="MainBoardCanvas" Focusable="True"
                        Height="{x:Static game:BoardConstants.FullCanvasLength}" MouseDown="BoardMouseDown"
                        RenderTransform="{Binding IsBoardInverted, Converter={StaticResource BoardInvertTransformConverter}}">
                    <Canvas.Resources>
                        <PowerEase x:Key="ease" EasingMode="EaseInOut" />
                        <Storyboard x:Key="fadeIn">
                            <DoubleAnimation Duration="{StaticResource duration}" To="1" Storyboard.TargetProperty="Opacity" />
                        </Storyboard>
                        <Storyboard x:Key="fadeOut">
                            <DoubleAnimation Duration="{StaticResource duration}" To="0" Storyboard.TargetProperty="Opacity" />
                        </Storyboard>
                        <Storyboard x:Key="fadeOutWithEvent" Completed="RemoveCompleted">
                            <DoubleAnimation Duration="{StaticResource duration}" To="0" Storyboard.TargetProperty="Opacity" />
                        </Storyboard>
                    </Canvas.Resources>

                    <!-- Row Column Labels -->
                    <ItemsControl ItemsSource="{Binding Board.RowColumnLabels}">
                        <ItemsControl.Resources>
                            <select:RowColumnIdSelector x:Key="rowColumnIdSelector" />
                            <RelativeSource x:Key="parent" Mode="FindAncestor" AncestorType="{x:Type ContentPresenter}" />
                            <Style x:Key="RowColumnIdCounterInvertStyle" TargetType="ContentControl">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <MultiBinding Converter="{StaticResource LabelCounterInvertTransformMultiConverter}">
                                            <Binding Path="DataContext.IsBoardInverted" RelativeSource="{RelativeSource AncestorType=Window}" />
                                            <Binding Path="Row" />
                                            <Binding Path="Column" />
                                            <Binding Path="Label" />
                                        </MultiBinding>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Style>
                        </ItemsControl.Resources>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas Width="{x:Static game:BoardConstants.FullCanvasLength}" Height="{x:Static game:BoardConstants.FullCanvasLength}" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Width" Value="{Binding Width}" />
                                <Setter Property="Height" Value="{Binding Height}" />
                                <Setter Property="Canvas.Bottom" Value="{Binding DistanceFromBottom}" />
                                <Setter Property="Canvas.Left" Value="{Binding DistanceFromLeft}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border>
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="White" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DataContext.Status, RelativeSource={RelativeSource AncestorType=Window}}" 
                                                             Value="{x:Static gameModel:Status.BlackTurn}">
                                                    <DataTrigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <ColorAnimation
                                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                                    To="Black"
                                                                    Duration="{StaticResource duration}" />
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </DataTrigger.EnterActions>
                                                    <DataTrigger.ExitActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <ColorAnimation
                                                                    Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                                    To="White"
                                                                    Duration="{StaticResource duration}" />
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </DataTrigger.ExitActions>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <ContentControl Content="{Binding}" Style="{StaticResource RowColumnIdCounterInvertStyle}">
                                        <ContentControl.ContentTemplateSelector>
                                            <StaticResource ResourceKey="rowColumnIdSelector" />
                                        </ContentControl.ContentTemplateSelector>
                                    </ContentControl>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <!-- Board fields -->
                    <ItemsControl ItemsSource="{Binding Board.Fields}">
                        <ItemsControl.Resources>
                            <conv:FieldBrushConverter x:Key="fieldBrushConverter" />
                        </ItemsControl.Resources>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas Width="8" Height="8" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Width" Value="1" />
                                <Setter Property="Height" Value="1" />
                                <Setter Property="Canvas.Bottom" Value="{Binding DistanceFromBottom}" />
                                <Setter Property="Canvas.Left" Value="{Binding DistanceFromLeft}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border>
                                    <Border.Background>
                                        <MultiBinding Converter="{StaticResource fieldBrushConverter}" Mode="OneTime">
                                            <Binding Path="Row" />
                                            <Binding Path="Column" />
                                        </MultiBinding>
                                    </Border.Background>
                                    <Rectangle Margin="0.075" Fill="SeaGreen" Opacity="0">
                                        <Rectangle.Style>
                                            <Style>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsTarget}" Value="True" >
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <StaticResource ResourceKey="fadeIn" />
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                        <DataTrigger.ExitActions>
                                                            <BeginStoryboard>
                                                                <StaticResource ResourceKey="fadeOut" />
                                                            </BeginStoryboard>
                                                        </DataTrigger.ExitActions>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Rectangle.Style>
                                    </Rectangle>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <!-- Board pieces -->
                    <ItemsControl ItemsSource="{Binding Board.Pieces}">
                        <ItemsControl.Resources>
                            <select:PieceSymbolSelector x:Key="symbolSelector" />
                            <RelativeSource x:Key="parent" Mode="FindAncestor" AncestorType="{x:Type ContentPresenter}" />
                            <Style x:Key="PieceCounterInvertStyle" TargetType="ContentControl">
                                <Setter Property="RenderTransform" Value="{Binding DataContext.IsBoardInverted, RelativeSource={RelativeSource AncestorType=Window},
                                    Converter={StaticResource PieceCounterInvertTransformConverter}}"/>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Style>
                        </ItemsControl.Resources>
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas Width="8" Height="8"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Width" Value="1" />
                                <Setter Property="Height" Value="1" />
                                <Setter Property="Opacity" Value="0" />
                                <Setter Property="Canvas.Bottom" Value="{Binding Position.DistanceFromBottom, Mode=OneTime}" />
                                <Setter Property="Canvas.Left" Value="{Binding Position.DistanceFromLeft, Mode=OneTime}" />
                                <Style.Triggers>
                                    <Trigger Property="Visibility" Value="Visible">
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <StaticResource ResourceKey="fadeIn" />
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                    </Trigger>
                                    <DataTrigger Binding="{Binding Removed}" Value="True" >
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <StaticResource ResourceKey="fadeOutWithEvent" />
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <ContentControl Content="{Binding}" Style="{StaticResource PieceCounterInvertStyle}" >
                                    <ContentControl.ContentTemplateSelector>
                                        <StaticResource ResourceKey="symbolSelector" />
                                    </ContentControl.ContentTemplateSelector>
                                    <ContentControl.Triggers>
                                        <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Duration="{StaticResource duration}" 
                                                        To="{Binding Position.DistanceFromBottom, NotifyOnTargetUpdated=True}"
                                                        Storyboard.Target="{Binding RelativeSource={StaticResource parent}}"
                                                        Storyboard.TargetProperty="(Canvas.Bottom)"
                                                        EasingFunction="{StaticResource ease}"/>
                                                    <DoubleAnimation Duration="{StaticResource duration}"
                                                        To="{Binding Position.DistanceFromLeft, NotifyOnTargetUpdated=True}"
                                                        Storyboard.Target="{Binding RelativeSource={StaticResource parent}}"
                                                        Storyboard.TargetProperty="(Canvas.Left)"
                                                        EasingFunction="{StaticResource ease}"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </ContentControl.Triggers>
                                </ContentControl>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Canvas>
            </Viewbox>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" DragCompleted="VerticalGridSplitter_DragCompleted" />

            <DockPanel Grid.Column="2">
                <Grid DockPanel.Dock="Top" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Background="LightGray" Margin="10" BorderBrush="White" BorderThickness="2" CornerRadius="5">
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation
                                                Duration="{StaticResource duration}"
                                                To="{Binding Status, Converter={StaticResource statusConverter}, NotifyOnTargetUpdated=True}"
                                                Storyboard.TargetProperty="BorderBrush.(SolidColorBrush.Color)" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                        <StackPanel Margin="5" Orientation="Horizontal">
                            <RadioButton Style="{StaticResource CustomRadioButtonStyle}" Width="40" Margin="5" Background="Transparent" Content="Play" Tag="Play" Name="PlayRadioButton" GroupName="ModeSelection" Click="PlayReCordReviewRadioButton_Click" IsChecked="{Binding SelectedAppModeValue, Converter={StaticResource AppModeConverter}, ConverterParameter={x:Static game:AppMode.Play}}"/>
                            <RadioButton Style="{StaticResource CustomRadioButtonStyle}" Width="40" Margin="5" Background="Transparent" Content="Record" Tag="Record"  Name="RecordRadioButton" GroupName="ModeSelection" Click="PlayReCordReviewRadioButton_Click" IsChecked="{Binding SelectedAppModeValue, Converter={StaticResource AppModeConverter}, ConverterParameter={x:Static game:AppMode.Record}}" />
                            <RadioButton Style="{StaticResource CustomRadioButtonStyle}" Width="40" Margin="5" Background="Transparent" Content="Review" Tag="Review" Name="ReviewRadioButton" GroupName="ModeSelection" Click="PlayReCordReviewRadioButton_Click" IsChecked="{Binding SelectedAppModeValue, Converter={StaticResource AppModeConverter}, ConverterParameter={x:Static game:AppMode.Review}}" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="1" Background="LightGray" Margin="10" BorderBrush="White" BorderThickness="2" CornerRadius="5" MouseDown="BoardMouseDown">
                        <Border.Triggers>
                            <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation
                                                Duration="{StaticResource duration}"
                                                To="{Binding Status, Converter={StaticResource statusConverter}, NotifyOnTargetUpdated=True}"
                                                Storyboard.TargetProperty="BorderBrush.(SolidColorBrush.Color)" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Border.Triggers>
                        <ContentControl Margin="0" Content="{Binding CurrentAppModeVM}"/>
                    </Border>
                </Grid>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition x:Name="ChessMovesListViewRow" Height="*" />
                        <RowDefinition x:Name="HorizontalSplitterRow" Height="5" />
                        <RowDefinition x:Name="ChessMovesNotesRow" Height="{Binding ChessMovesNotesRowHeight}" />
                    </Grid.RowDefinitions>
                    <Border CornerRadius="5" Background="LightGray" Margin="10,5,10,10" x:Name="ChessMovesListViewBorder" Focusable="True" MouseDown="BoardMouseDown">
                        <ListView x:Name="myListView" Grid.Row="0"
                        ItemsSource="{Binding Path=Board.ChessMoveSequence.ChessMoves}"
                        Height="Auto" Margin="10 5 10 10 " Width="Auto"
                        VerticalAlignment="Top" Background="AliceBlue">
                            <ListView.Resources>
                                <select:PieceSelectorForMoveSequenceListView x:Key="pieceSelectorForMoveSequenceListView" />
                            </ListView.Resources>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Width="15" VerticalAlignment="Center" Text="{Binding MoveNumber}" 
                                               HorizontalAlignment="Center" Margin="10" FontWeight="Bold"/>
                                        <ContentControl Content="{Binding}">
                                            <ContentControl.ContentTemplateSelector>
                                                <StaticResource ResourceKey="pieceSelectorForMoveSequenceListView" />
                                            </ContentControl.ContentTemplateSelector>
                                        </ContentControl>
                                        <TextBlock Width="30" VerticalAlignment="Center" Text="{Binding PieceColor}" Margin="10" />
                                        <TextBlock Width="30" VerticalAlignment="Center" Text="{Binding PieceName}" Margin="10" />
                                        <TextBlock Width="20" VerticalAlignment="Center" Text="{Binding SourceString}" Margin="10"/>
                                        <TextBlock Width="20" VerticalAlignment="Center" Text="{Binding TargetString}" Margin="10"/>
                                        <TextBlock Width="65" VerticalAlignment="Center" Text="{Binding ShortDescription}" Margin="10"/>
                                        <TextBlock VerticalAlignment="Center" Text="{Binding GameAndUpdateInfo}" Margin="10"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Border>
                    <GridSplitter Grid.Row="1" Height="5" VerticalAlignment="Stretch" DragCompleted="HorizontalGridSplitter_DragCompleted" />
                    <Border Grid.Row="2" CornerRadius="5" Background="LightGray" Margin="10" 
                            Visibility="{Binding IsChessMovesNotesRowCollapsed, Converter={StaticResource BoolToVisibilityConverter}}" >
                        <Grid>
                            <TextBox Margin="5" Name="TitleNotesTextBox" FontSize="15" Text="{Binding TitleNotesText, Mode=TwoWay, 
                                UpdateSourceTrigger=PropertyChanged}" BorderThickness="0" AcceptsReturn="True" 
                                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="LightGray" 
                                     VerticalAlignment="Top"  >

                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="GotFocus">
                                        <i:InvokeCommandAction Command="{Binding TitleNotesGotFocusCommand}" />
                                    </i:EventTrigger>
                                    <i:EventTrigger EventName="LostFocus">
                                        <i:InvokeCommandAction Command="{Binding TitleNotesLostFocusCommand}" />
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </TextBox>
                            <TextBlock IsHitTestVisible="False" Name="PlaceHolderTextBlock"
                                   Text="{Binding PlaceHolderTextForTitleNotesTextBox}"
                                   VerticalAlignment="Top" TextWrapping="Wrap" Foreground="Gray"
                                   Background="Transparent" Visibility="{Binding PlaceHolderTextForTitleNotesTextBoxVisibility}"
                                   HorizontalAlignment="Left" Margin="7 4 7 7" FontSize="20">
                            </TextBlock>
                        </Grid>
                    </Border>
                </Grid>
            </DockPanel>
        </Grid>
    </DockPanel>
</mah:MetroWindow>